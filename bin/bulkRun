#!/bin/bash

cmd="killall java; cd ~/workspace555/asg1/src && java node.Node 5656 ../new 5000 5" 

ssh asparagus $cmd     
ssh beet $cmd 
ssh cantaloupe $cmd   
ssh cauliflower $cmd 
ssh corn $cmd 
ssh dillweed $cmd   
ssh horseradish $cmd  
ssh lettuce $cmd  
ssh parsnip $cmd  

`` 
